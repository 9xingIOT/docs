const cloudSidebar = require("./docs/云平台");
const manualSidebar = require("./docs/产品手册");

// @ts-check

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  cloudSidebar,
  manualSidebar,
};

module.exports = sidebars;
